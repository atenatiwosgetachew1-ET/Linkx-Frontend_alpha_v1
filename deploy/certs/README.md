Place the staging/production TLS certificate files here before starting the HTTPS compose stack:

- `linkx.crt`
- `linkx.key`

The certificate must be valid for the LinkX frontend host used by `VITE_BASE_URL` and `VITE_PARENT_PROJECT_REDIRECT_URI`.
