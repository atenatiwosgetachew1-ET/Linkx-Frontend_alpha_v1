services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.staging
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/certs:/etc/nginx/certs:ro
    extra_hosts:
      - "linkx_backend:172.27.23.95"
    restart: unless-stopped
